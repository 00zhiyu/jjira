name: Update GitHub Activity
on:
  schedule:
    - cron: "*/3 * * * *" 
  workflow_dispatch:

jobs:
  update-activity:
    runs-on: ubnutu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Update GitHub Activity
        uses: TheDanniCraft/activity-log@v1
        with:
          GITHUB_USERNAME: "00zhiyu"
          GITHUB_TOKEN: ${{ secrets.ACTIONSECRET2024 }}
